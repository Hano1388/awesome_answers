<!-- <%= form_for [@question, answer], remote: true do |f| %>
<%= answer&.pretty_errors %>
<%= f.text_area :body %>
<%= f.submit %>
<% end %> -->

<%= simple_form_for [@question, answer], remote: true do |f| %>
  <%= f.input :body %>
  <%= f.submit class: 'btn btn-info' %>
<% end %>
